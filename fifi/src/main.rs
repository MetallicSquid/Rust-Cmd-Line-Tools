// This is a command line tool which scans the user's file system for a file based on a piece of information (either the file's name or a piece of metadata).

// All of the neccessary crates.
use std::{path, fs::metadata, collections::HashMap};
use structopt::StructOpt;
use chrono::{offset::Local, DateTime};












fn main() {
    println!("Hello, world!");
}
